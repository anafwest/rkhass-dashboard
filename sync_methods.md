# جدول طريقة السحب المعتمدة (adopted sync method)

> المنهج المعتمد ابتداءً من 2026-09-13: **مزامنة تزايدية ذكية** — لا نعيد سحب البيانات الموجودة؛
> نضيف الجديد ونحدّث حالة القائم، ونفحص المستحدث فقط. يُستثنى سحب كامل عند الضرورة فقط.

## 1) جدول السحب حسب المصدر

| المصدر | البيانات | السكربت المعتمد | الطريقة | الزمن المعتاد | الناتج |
|---|---|---|---|---|---|
| **BLS** (شاشة 8510 طلبات رخص البناء) | `data.xlsx` | `update_bls_delta.py` | تزايدية: نافذة زمنية من آخر تاريخ مراجعة (افتراضي حتى نهاية النطاق)، سحب الشهور بالتوازي، دمج (إضافة الجديد + تحديث حالة الموجود) | ~1–5 دقائق | `data.xlsx` + `data_prev.xlsx` احتياطي |
| **UPS** (بوابة رخص البناء) | `ups_requests.xlsx` | `ups_sync_fast.py` | موازية: تمرير على كل الصفحات (154) عبر 5 تبويبات لفحص كل الحالات + إضافة الجديد | ~1.5–2 دقيقة | `ups_requests.xlsx` + `ups_prev.xlsx` احتياطي |

### بدائل / حالات خاصة

| الحالة | الاستخدام |
|---|---|
| `data.xlsx` مفقود أو فجوة كبيرة | السحب الكامل: `run_fast.py` (BLS) |
| بوابة UPS طلبت دخولاً | سجّل الدخول في التبويب المفتوح ثم أعد التشغيل |
| تنبيه: البوابة بلا فلتر حالياً | لمزيد السرعة لاحقاً: فلتر `createdOn` (تاريخ الطلب) متاح في الواجهة |

## 2) جدول رفع GitHub

| خطوة | محرك | العلامة |
|---|---|---|
| BLS | `update_bls_delta.py` → commit+push تلقائي | "تم الرفع لـ GitHub" أو "لا توجد تغييرات" |
| UPS | `ups_sync_fast.py` → commit+push تلقائي | نفس العلامة في `ups_sync_log.txt` |

## 3) جدول الجدولة اليومية

| الوقت | المهمة | المحرك |
|---|---|---|
| 08:00 / 11:00 / 14:00 | جولة BLS + UPS | `scheduled_bls.py` |
| نهاية اليوم | خط الإنتاج الكامل + تقرير يومي | `run_all.py` → `generate_report.py` → `daily_report.md` |
| أسبوعياً | مراجعة `monitor_log.txt` و`scheduled_run.log` | يدوي |

## 4) خيارات متغيرات البيئة

| المتغير | التأثير | مثال |
|---|---|---|
| `BLS_WINDOW_END` | نهاية نافذة سحب BLS | "اليوم فقط" = end بتاريخ اليوم (1448/04/01) |
| `BLS_OVERLAP_DAYS` | أيام تراكب احتياطية قبل آخر مراجعة | `0` (افتراضي) |
| `BLS_WORKERS` / `UPS_WORKERS` | عدد التبويبات المتوازية | `6` / `5` |

## 5) قواعد التشغيل

1. `update_bls_delta.py` و`ups_sync_fast.py` يرفعان تلقائياً لـ GitHub عند التغيير.
2. الملف الاحتياطي (`_prev.xlsx`) يُنشأ قبل كل حفظ جديد.
3. عند تعارض تشغيل مزدوج: قفل `*.lock` فيمنع التزامن.
4. السجلات: `delta_log.txt` (BLS) و`ups_sync_log.txt` (UPS) و`scheduled_run.log`.